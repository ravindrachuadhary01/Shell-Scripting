#!/bin/bash
# This script prints "Hello, World!" to the console.

echo "Hello, World!!!!!!!" 

<<< cmt
This script is a simple bash script that prints "Hello, World!" to the console. It uses the echo command to display the message. The script can be executed in a terminal or command prompt by running the command `bash 01_script.sh` (assuming the script is saved as 01_script.sh).

cmt
